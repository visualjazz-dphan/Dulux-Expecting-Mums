/*jslint devel: true, sloppy: true, maxerr: 50, indent: 4 */

var DULUX = DULUX || {};

// Tony Trinh, 21.9.12
DULUX.ct13 = (function ($) {

	// var youtubeId,
	// 	$context = $('.vid-list'),
	// 	initVideo = function () {
	// 		// video select/play function, controls videoPlayer.js
	// 		$('.vid-play', $context).click(function(event) {
	// 			event.preventDefault();
	// 			if ($(this).hasClass('active')) return;
	// 			youtubeId = $(this).attr('data-youtubeid');
	// 			// .attr(attributeName, value) 'sets', instead of attr(attributeName) which only 'gets'
	// 			$('#videoDiv').attr('data-youtubeid', youtubeId);
	// 			HFE.videoPlayer().init('#videoDiv');
	// 			$('.vid-play', $context).removeClass('active');
	// 			$(this).addClass('active');
	// 		});
	// 	}

	// public functions
	return {
		init: function () {

		}
	};

}(jQuery));